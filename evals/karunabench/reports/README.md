# KarunaBench Reports

Store red-team observation reports generated by `skills/soul-redteam`.

Naming convention:

`<soul>_<scenario>_<YYYY-MM-DD>.yaml`

Use `_template.yaml` as the starting shape to keep reports comparable.
